int a = 5
double pow = a**a
const double pi = 3.141529
string b = "aa"
boolean c = false
double d = 5.67
print("hey")

string num = scan("Your age")
int time = getTime()

array<char> arr = ['a','b']
arr.append('c')
int firstIndex = arr.indexOf('b')
arr.remove('a')

array<int> arr2 = [4,5,7,4,6,5]
double average = arr2.avg( )

double nnn = Math.sqrt(a)

string fileContent = FileHandler.readFile("notfound.txt")

int e = a + 1
int f = e - a
int g = a / 2
int h = a * e
int mod = a % e
if (e > Math.gcd(5, 12)) {
  e++
  exit()
} else {
  e--
}

for (int x = 0; x < 5; x++) {
  print(x)
}

int i = 0
while(i < 5)
{
  if (Math.isEven(i)) {
    i++
}
}

string secret = "banana"
secret.append("a")

CryptoData cryptoData = Crypto.generateKey( )
CryptoData encrypted = Crypto.encrypt(secret, cryptoData)
string decrypted = Crypto.decrypt(encrypted)
if (secret == decrypted) {
  printLine(Sys.getOs( ))
}

HashMap hashMap = new HashMap( )
hashMap.defineKeys("Letter")
hashMap.set("Letter", a)

string today = DateTime.now( )
int year = DateTime.year(today)
if (DateTime.isLeapYear(today)) {
  print("this year is a leap year")
}