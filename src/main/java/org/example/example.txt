int a = 5
double pow = a**a
const double pi = 3.141529
string b = "aa"
boolean c = false
double d = 5.67
printLine("hey, bro")

string age = scan("Your age")
int ageCasted = TypeCaster.stringToInt(age)

string time = getTime()
int timeInInt = TypeCaster.stringToInt(time)

array<char> arr = ['a','b']
int firstIndex = arr.indexOf('b')
char e = arr.get(0)

List<int> list1 = new List(4,3,5,4,6,4,3,5,4,6,6)
list1.add(2)
list1.removeDuplicateOf(6)

string fileContent = FileHandler.readFile(b)

if (e > Math.gcd(5, 12)) {
  e++
} else {
  e--
}

for (int x = 0; x < 5; x++) {
  print(x)
}

int i = 0
while(i < 5)
{
  if (Math.isEven(i)) {
    i++
}
}

string secret = "banana"
secret.append("a")

CryptoData cryptoData = Crypto.generateKey( )
CryptoData encrypted = Crypto.encrypt(secret, cryptoData)
string decrypted = Crypto.decrypt(encrypted)
if (secret == decrypted) {
  exit()
}

PerfectHashMap<char> hashMap = new PerfectHashMap( )
hashMap.defineKeys("Letter")
hashMap.set("Letter", e)

string today = DateTime.now( )
if (DateTime.isLeapYear(today)) {
  print("this year is a leap year")
}

int zz
zz = 3333
zz = 32